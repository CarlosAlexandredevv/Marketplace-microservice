# Spec: integração users-service ↔ api-gateway (marketplace-ms)

O conteúdo desta integração foi **consolidado e atualizado** em **[06-gateway-integration.md](./06-gateway-integration.md)** (proxy unificado via `ProxyService`, alinhado ao padrão do products-service no gateway).

Utilize **apenas** o documento **06** como fonte de verdade para requisitos e critérios de aceite da integração gateway ↔ users-service.
